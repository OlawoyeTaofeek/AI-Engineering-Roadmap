"""DistributedDataParallel training entrypoint. Launch with:
    torchrun --nproc_per_node=<num_gpus> ddp_train.py
"""
raise NotImplementedError("TODO: init_process_group, wrap model in DDP, use DistributedSampler")
