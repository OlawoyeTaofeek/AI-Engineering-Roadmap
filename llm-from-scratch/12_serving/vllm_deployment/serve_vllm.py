"""Serve the trained checkpoint via vLLM.

Usage:
    pip install vllm
    python serve_vllm.py --model checkpoints/final_model
"""
raise NotImplementedError("TODO: vllm.LLM(...) or `vllm serve` CLI invocation wrapper")
