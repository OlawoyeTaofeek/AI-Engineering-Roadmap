"""Minimal FastAPI inference server around the trained model."""
raise NotImplementedError("TODO: POST /generate endpoint -> tokenize, run generate loop, "
                           "return text; wire in batching.py and streaming_response.py")
